"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const React = require("react");
const useRenderProp_1 = require("./useRenderProp");
const useAdopt = (map) => {
    const keys = Object.keys(map);
    const fragments = [];
    const result = {};
    keys.sort();
    for (const key of keys) {
        const [fragment, value] = useRenderProp_1.default(map[key]);
        fragments.push(React.cloneElement(fragment, { key }));
        result[key] = value;
    }
    return [React.createElement(React.Fragment, null, ...fragments), result];
};
exports.default = useAdopt;
